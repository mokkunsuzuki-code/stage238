name: stage238-slsa-policy

on:
  push:
    branches: [ main ]
  workflow_dispatch:

permissions:
  contents: read
  id-token: write
  attestations: write

jobs:
  slsa-governed-build:
    uses: ./.github/workflows/reusable-slsa-build.yml
    with:
      artifact_name: stage238-source-bundle
      sbom_name: stage238-sbom.spdx.json
